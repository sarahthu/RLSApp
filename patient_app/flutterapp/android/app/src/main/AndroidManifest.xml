<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Berechtigung zum Anzeigen von Benachrichtigungen
      (erforderlich ab Android 13)
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Berechtigung für exakte Alarme.
      Wird benötigt, um Erinnerungen zu einem genau definierten Zeitpunkt
      auszulösen (relevant ab Android 14).
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!--
      Ermöglicht das erneute Planen von Benachrichtigungen
      nach einem Geräte-Neustart.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Standard-Berechtigung für Vibrationen bei Benachrichtigungen.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="flutterapp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!--
          Haupt-Activity der Flutter-App.
          singleTop verhindert das mehrfache Erstellen der Activity
          beim Antippen einer Benachrichtigung.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!--
              Standard-Theme für den Flutter-Startbildschirm.
            -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
          Receiver für geplante Benachrichtigungen.
          Wird vom flutter_local_notifications Plugin benötigt.
        -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <!--
          Receiver zum erneuten Planen von Benachrichtigungen
          nach einem Geräte-Neustart oder App-Update.
        -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!--
          Receiver für Benachrichtigungs-Aktionen (z. B. Button-Klicks).
          Auch wenn aktuell keine Actions genutzt werden, schadet er nicht.
        -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />

        <!--
          Erforderliche Meta-Information für das Flutter-Embedding.
        -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

   
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
